# Alderpoint DNS V2 — Go/Svelte Route-by-Route Parity Matrix

**2026-08-28 correction, read this before trusting any `pyanalytics`/`analyticssnapshot`/
`rawquerylog` prose below**: Python has been fully decommissioned (see `CUTOVER.md`; the sole live
appliance is `apdns-go-live`, Go alone on `:8443`/`:53`). Every mention below of a Go->Python
analytics compatibility boundary, a published snapshot Python's own worker produces, or
`worker-heartbeats/analytics-worker.json` describes an **architecture that no longer exists** --
left in place as history, not current fact. Analytics today is `internal/dnsanalytics`
(2026-08-28): a real dnstap Frame Streams receiver fed directly by dnsdist's own query-event
logging, one SQLite table (`query_events`), answering every `/api/analytics/*`/`/api/statistics/*`
handler through the same `pyanalytics.Bucket`/`AnalyticsHealth` shapes for zero frontend churn.
`internal/rawquerylog`'s Parquet-reader boundary is likewise gone -- Top Blocked Domains and Query
Log both read `query_events` directly now. See `alderpointdns-go-live-analytics-dnstap` /
`alderpointdns-go-dnstap-ingestion-fix` memory for the full history, including a real, previously
unresolved intermittent silent ingestion stall (dnsdist's fstrm connection could go idle --
established, but no frames, no error either side) that is now root-caused and fixed: `Writer` runs
its own accept loop (retaining live connections instead of hiding them behind the dnstap library's
own socket wrapper) and a watchdog cross-checks an independent BIND-traffic probe (a real
`apdns-hostagent` `cache.status` RPC, entirely outside the dnsdist/dnstap path) to tell a genuine
stall apart from a quiet network, then forces the stalled connection closed -- which turns
dnsdist's next write into a real error, the one thing its own FrameStream logger already reacts to
by reconnecting. Every stall/recovery is durably logged to a new `ingestion_events` table and
surfaced through `/api/health`'s `analytics.ingestion_*` fields, which now demote `status` to
`degraded` on a real stall instead of the old heartbeat-only check (which could never detect this
failure class, since the accept/decode loop's heartbeat keeps ticking regardless of whether any
frame ever arrives).

**Status:** authoritative tracking document for the complete Go/Svelte management-application
rewrite. Supersedes `MILESTONE_1_REPORT.md`'s framing of the existing Blocklists/Local DNS work as
a completed frontend milestone — that work is a **foundation slice** (proves the architecture:
Go control-plane API shape, Svelte 5 + strict TS toolchain, auth/session/CSRF, staged-deploy
pattern, isolated preview pipeline). It is not frontend parity. No route below may be marked
`done` until it is real, tested, and owner-accepted end to end — see `docs/v2/v2-roadmap.md`'s
"Definition Of Workflow Parity": a model, a route, or a page with the same title is not parity.

**Source of truth for the Python surface:** `app/v2/webapp.py` (single FastAPI module, 116 routes,
no HTML templates — it serves one SPA shell, `app/v2/ui/index.html`, plus `app/v2/ui/app.js`
(3701 lines, all 20 page renderers + router), `app/v2/ui/data-grid.js` (shared table behavior),
`app/v2/ui/app.css`). V1's `web/templates/*.html` (25 templates, `app/webapp.py`, 141 routes) is a
separate legacy app and is **not** part of this rewrite's target surface — V2 already superseded it
as the functional reference, per the roadmap.

**Testing infrastructure:** `go/tests/acceptance.py` (black-box API, language-neutral) and
`go/tests/browser/chromium_smoke.mjs` (real headless Chromium via `puppeteer-core` against
`/usr/bin/chromium` -- no bundled download, no mocking) are both run against every coherent slice
before it's deployed to `:10443`, not deferred to the end. Run the Chromium harness with
`cd go/tests/browser && npm install && node chromium_smoke.mjs <https-base-url> <username> <password>`
against a **fresh** (`setup_required: true`) instance -- it drives real setup/login and must not be
pointed at the live owner preview, whose credentials are Alex's. `go/tests/browser/perf_measure.mjs`
(same invocation shape) measures real client-side navigation latency. `go/tests/browser/hostagent_smoke.mjs`
(same invocation shape) covers the Cache/Replication/Network Configuration/Logs/Software Updates
pages specifically, since exercising them for real requires a deployment with a running
`apdns-hostagent` alongside the web binary (see `internal/hostagentd`'s doc comment), not just any
fresh instance.

**Host-control boundary (Cache, Replication, Network Configuration, Logs, Software Updates):**
these five areas need real host-level access (BIND's rndc port, the host network stack, the systemd
journal, this binary's own file) that the web control-plane process itself must never hold directly
-- granting it directly would mean either running the whole web process as root or mounting
Python's private `control.db`/secrets into it. Both are refused. Instead, `apdns-hostagent`
(`cmd/apdns-hostagent`, `internal/hostagentd`) is a separate, minimal, root-owned daemon: a Unix
socket, authorized per-connection by kernel-verified `SO_PEERCRED` (never anything the client
asserts) against one configured allowed UID, exposing a fixed compile-time allowlist of named
operations only -- there is no generic shell/command endpoint (`internal/hostagentd/server_test.go`
proves `exec`/`shell`/`run`/`command`/`eval`/`system.exec` are all rejected as unknown ops, the same
as any other made-up name). Every request, allowed or rejected, is recorded to an append-only
JSON-lines audit log. Proven not just with in-process unit tests (which structurally share one UID
and so can never exercise a real cross-UID rejection) but with a genuine two-real-UID end-to-end
integration test -- root running the agent, a real unprivileged system user running the web binary
-- which caught a real bug (an 0600 socket file that silently blocked the legitimate client before
authorization ever ran) no unit test could have found.

**Deployed-UID integration defect found and fixed (real, live, on :10443):** once the container
actually ran as its real unprivileged UID (see above) instead of root, Dashboard analytics and Top
Domains broke outright ("Analytics degraded: unable to open database file (14)") -- root-caused in
`internal/pyanalytics`, `internal/deployperm`'s doc comment, two real layers: (1) Python's
`analytics`/`certs` mount directories are genuinely group-restricted (`_dnsdist:bind`, mode 750/640)
on the host, and podman's `--user UID:GID` does not automatically grant a container process any of
that host user's real supplementary group memberships -- fixed with `--group-add 103` (the real
`bind` GID) on the container's own `podman run`, the narrowest possible grant (one specific existing
GID, not a broader mount or permission change). (2) Python's live `aggregates.db` is WAL-mode
(confirmed live), which needs to open/create a `-shm` coordination file even for a read; SQLite's
`mode=ro` alone doesn't exempt WAL databases from that, so a genuinely read-only mount still failed
the same way -- fixed with SQLite's own `immutable=1` URI parameter, telling this reader (whose
whole contract is already "tolerant of slightly-stale data, never blocking") to skip WAL
change-detection and read the main file directly. Root always had full read-write access regardless
of either issue, masking both until the container ran unprivileged for real. A permanent, real
regression test (`internal/deployperm`, a genuine two-real-UID subprocess re-exec, not a mock)
reproduces the exact live error against a real WAL-mode database and proves both fixes are actually
necessary (each removed independently makes the test fail again, with the real corresponding SQLite
error). Verified fixed end-to-end via real Chromium against a disposable instance mounting the same
real live `aggregates.db`/`server.crt`, running as UID 996 with `--group-add 103`: real chart data,
real Top Domains rows, zero "degraded" text anywhere on the page.

**DNS Runtime Compiler (Local DNS, Custom Rules, Blocklists, DNS Settings, DNS Transports):**
closes the "compiled Go state doesn't actually affect live DNS" gap every one of these rows
previously disclosed. Two new pieces:

- `internal/dnscompile` -- a pure, dependency-free, deterministic Go function that turns
  already-loaded control-plane state into real BIND `named.conf` and real dnsdist Lua config text.
  Architecture matches Python's own real, live design (read directly from `app/v2/dnsdist_policy_runtime.py`/
  `app/v2/bind_gen.py`, not guessed): dnsdist is the primary policy-enforcement layer (blocking,
  local DNS, upstream routing, packet cache); BIND is wired underneath it purely as a shared
  recursive-cache backend for the default upstream profile's plain/DoT traffic. Deliberately
  narrower than Python's real per-network compiler, disclosed in the package's own doc comment:
  global policy only (matches where `internal/policy` itself currently is -- no per-network
  "effective policy" resolution engine exists yet), one default upstream profile, no domain
  routing, no SafeSearch/ECS, DoT/DoH compiled but not DoQ/DoH3/DNSCrypt. Every generated config is
  validated against the real installed `dnsdist --check-config`/`named-checkconf` binaries in the
  package's own tests (26 tests, one per policy type -- local DNS record types, each blocking
  response mode, regex allow/block precedence, rewrite rules, plain/DoT/DoH default-pool routing,
  the zero-managed-upstream BIND fallback, DoT/DoH listener wiring, BIND forwarders/native
  recursion/rndc controls block).
- `internal/hostagentd/ops_dnsruntime.go` -- the real stage -> validate -> promote -> reload ->
  health-check -> automatic-rollback pipeline, exposed as two named host-agent operations
  (`dns_runtime.status`, `dns_runtime.promote`) -- no generic command execution, matching every
  other host-agent operation's own contract. The web process compiles and sends the dnsdist config
  text (no secrets involved); the host-agent compiles BIND's own config itself, since it needs an
  rndc HMAC key generated fresh at agent startup and never handed to the unprivileged web process.
  Both configs must validate before either promotes; promotion backs up whatever was live; reload is
  a real `rndc reconfig` for an already-running BIND (or a fresh start) and a real dnsdist process
  restart (Lua config is only read at start); a real-`dig` health check against a fixed,
  compile-time-constant marker record (never user-controlled) proves the *just-promoted* config is
  actually the one live and answering, not merely that the old process is still up -- any
  validation, promotion, reload, or health-check failure restores and reloads the previous
  known-good runtime and reports the exact stage/error, never a silent partial promotion. Proven
  end-to-end with real, disposable BIND+dnsdist processes this test suite brings up and tears down
  itself (never Python's `apdns-v2-preview`): a real Local DNS answer, a real blocked-domain RCODE,
  real upstream routing to a controlled fake upstream (no internet-egress dependency), and a real
  rollback that restores the prior config's real answer after an engineered health-check failure (9
  tests in `internal/hostagentd`, plus `internal/dnsruntime`'s own end-to-end test proving the full
  chain from a row in a real SQLite DB through to a real `dig` answer).

`internal/dnsruntime.Orchestrator` is the web-side glue: gathers state from the same service
objects `httpapi` already holds (no raw SQL duplicated), compiles, and calls the host-agent.
Wired into Local DNS's create/update/delete (auto-triggers on every mutation, reported inline as a
`dns_runtime` response field, matching `internal/localdns`'s own pre-existing `stageAndPromote`
convention); Custom Rules, Blocklists, DNS Settings, and DNS Transports are all read by the
compiler but not yet auto-triggered from their own mutation handlers -- reachable today via the new
DNS Runtime page's manual "Apply Runtime Changes" action (`GET/POST /api/dns-runtime/{status,apply}`),
a real, bounded, disclosed scope narrowing rather than leaving them unwired entirely. Deployed and
verified end-to-end against a live two-process (`apdns-hostagent` + web binary, real two-UID
privilege separation) instance, including through the actual Svelte UI (4 Chromium checks in
`hostagent_smoke.mjs`: status renders, Apply performs a real compile+promote+report cycle).

**Python state migration (`internal/pymigrate`, `alderpointdns-go import-python`):** the audited,
one-time import path from Python's real `control.db` into this Go control plane's own native
schema -- never a permanent proxy, never a runtime dependency on Python being reachable afterward,
never touching `admins`/`sessions`/secrets or anything SecretStore-referenced. Scope is deliberately
bounded to the five tables `internal/dnscompile` actually consumes (disclosed in the package's own
doc comment, not silently narrowed): `local_dns_records`, `upstream_profiles`+`upstream_endpoints`,
`dns_transport_settings` (enable/port/path only, never `dnscrypt_settings`), `policy_layers` global
scope only, and `blocklist_subscriptions` (registration only -- the actual domain list is always
pulled fresh from its real URL by Go's own scheduler, never copied). Every table not migrated is
explicitly listed in the tool's own report, not silently dropped. A real pre-migration snapshot
(the same `internal/backup` VACUUM INTO mechanism the Backup & Restore page itself uses) is taken
before any real write, and rollback is the same real, already-tested transactional restore path
(mandatory safety-backup-before-restore included). Every row, imported or not, is written to an
append-only JSON-lines audit log. Proven with 6 tests: dry-run writes nothing, a real import
actually creates real rows (including preserving a source row's disabled state), running the import
twice never duplicates, rollback restores the exact pre-migration state, and a dry-run against the
*actual* live owner-preview `control.db` (read-only, never written to) -- which caught two real
discrepancies before they could cause a silent bug: Python's global policy row uses
`scope_ref='singleton'`, not `'global'` (Go's own convention) -- fixed to match on `scope='global'`
alone; and running the real import against the actual `:10443` database surfaced two pre-existing
malformed test-artifact upstream profiles (addresses missing a port, left over from earlier testing
this session) that the DNS runtime compiler correctly rejected rather than silently accepting --
cleaned up, not worked around. **Performed for real** against the actual owner-preview's real
`control.db` (read-only source) into the real `:10443` Go database: 47 local DNS records, 3 upstream
profiles, 1 global policy layer, and 20 blocklist subscriptions imported, then promoted through the
real DNS runtime compiler and verified with real `dig` queries answering with the real imported
data (e.g. `3d-printer.mylan.network` -> `192.168.32.15`) -- then rolled back for real (confirmed
the data disappeared) and re-imported to restore the final state, proving the full
import -> drive real DNS behavior -> roll back -> re-import cycle end-to-end, not just against
disposable fixtures.

**Performance (measured 2026-08-26, this build server, real Chromium + curl, not estimated):**

| Metric | Target | Measured |
|---|--:|--:|
| Cold load to first usable screen | < 2000 ms | 1038 ms |
| Route click -> new heading visible (p50, 4 routes) | < 50 ms | 17-29 ms |
| Route click -> new heading visible (max observed) | -- | 38 ms |
| Idle RSS after setup+login | < 25 MB (known-missed, see `MILESTONE_1_REPORT.md`) | 33.5 MB |
| `GET /api/health` (p95 / p99, n=150) | < 25 ms / < 50 ms | 6.95 / 9.33 ms |
| `GET /api/dashboard/summary` (p95 / p99) | < 25 ms / < 50 ms | 7.13 / 8.07 ms |
| `GET /api/analytics/timeseries` (p95 / p99) | < 25 ms / < 50 ms | 8.18 / 8.87 ms |
| `GET /api/analytics/top-domains` (p95 / p99) | < 25 ms / < 50 ms | 17.15 / 18.40 ms |
| `GET /api/blocklists` (p95 / p99) | < 25 ms / < 50 ms | 7.00 / 7.29 ms |
| `GET /api/local-dns` (p95 / p99) | < 25 ms / < 50 ms | 6.91 / 7.20 ms |

All gates met except idle RSS, which was already a known, root-caused, disclosed miss from
Milestone 1 (146 MB -> 36.4 MB fix landed then; 33.5 MB here is consistent, not a new regression --
no auth/session code changed this slice). `top-domains` is the slowest endpoint (a `GROUP BY`
across 24 hours of hour-granularity `dimension_counts` rows) but is still ~3x under its p99 budget.

**Authoritative target surface** (per the governing task): the union of (a) what Python V2 actually
does today, (b) required V1.1.1 workflow semantics per `docs/v2/v1-workflow-parity-contract.md`,
(c) accepted owner corrections already recorded in that contract and in `docs/v2/v2-roadmap.md`'s
"Locked Owner Decisions", and (d) newly approved V2-only functionality. Where Python V2 itself has a
known, documented defect (e.g. a11y/nav bugs already fixed post-audit, raw-ID leaks), the fix is the
target, not the defect — literal bug-for-bug parity is not the goal.

**Status columns legend:** `not started` / `in progress` / `api done` / `ui done` / `done (browser-tested)` / `done (owner-accepted)`.

## Legend for shared mechanics (referenced per-row instead of repeated)

- **Shared data grid**: client-side sort (numeric-aware), drag-resize (persisted per grid id),
  capped row count with overflow notice, no server pagination in Python V2 today, auto-wrap in
  scrollable container. One Svelte component to build once, used by every table row below.
- **Timestamp modes**: Browser Local / Appliance Time / UTC, instant reformat, no refetch. One
  shared mechanism (Administration page owns the preference; every other page consumes it).
- **CSRF/session**: already implemented in the Go foundation slice (`internal/auth`) — reused by
  every route below, not re-derived per page.
- **Stale-response/mutation-race protection**: already implemented for Blocklists
  (`staleGuard.ts`) — the pattern generalizes to every other page's mutations, not rebuilt per page.

---

## Pre-auth

| Page | Route | Python API | Controls | Go API | Svelte | Browser test | Owner accepted |
|---|---|---|---|---|---|---|---|
| Setup / Login | (no nav; gated by `/api/setup/status`) | `GET /api/setup/status`, `POST /api/setup`, `POST /api/login`, `POST /api/logout`, `GET /api/session` | Shared auth form, password show/hide, live mismatch validation | done | done | in progress (acceptance.py covers it; no Chromium yet) | not started |

## Dashboard (top-level, no group)

| Page | Route | Python API | Controls | Tables | Charts/live | Polling | States | Go API | Svelte | Browser test | Owner accepted |
|---|---|---|---|---|---|---|---|---|---|---|---|
| Dashboard | `dashboard` | `GET /api/health`, `/api/system/status`, `/api/replication/health`, `/api/discovery/status`, `/api/analytics/recent`, `/api/clients`, `/api/discovery/observed-clients`, `/api/upstreams`, `/api/analytics/timeseries`, `/api/analytics/live-activity`, `/api/analytics/{top-domains,top-blocked-domains}` | Refresh; range select (Live/1h/24h/7d); top-domains mode select; live pause/resume; **card add/remove/reorder + persistence (not yet in Python V2 either — new V2 requirement)** | Upstreams mini-table, Top-domains table, client mini-list | DNS Activity SVG time-series (queries + blocked), live 1s mode | `setInterval` 1000ms in live mode only, cleared on route leave | degraded banner, live status states (Connecting/Connected/Paused/Reconnecting/Degraded), empty states | **done for the boundary this session closed (2026-08-27)** -- `GET /api/dashboard/summary` (Go-native: blocklists + local-dns + `analytics_available`) plus `GET /api/analytics/{timeseries,live-activity,top-domains,top-blocked-domains}` real, backed by `internal/pyanalytics` (read-only SQLite read of the published analytics snapshot; see that package's doc comment). `top-blocked-domains` backed by `internal/rawquerylog` whenever `-query-log-dir` is configured, honestly degraded when it isn't. **Upstreams mini-table, client mini-list: no longer missing** -- the "policy boundary" this row was blocked on is now native Go (`internal/clients`, `internal/upstreams`, `GET /api/clients/observed`), so the Dashboard reuses those existing endpoints directly; no new backend surface was needed for this row | **done for what's built (2026-08-27)** -- `DashboardView.svelte`: real cards (Blocklists, Local DNS, DNS Activity chart, Top Domains, Top Blocked Domains, **Clients mini-panel** (managed clients + not-yet-managed observed addresses, matching V1's own managed+observed mini-list semantics, "Manage" link routes to the Clients page rather than duplicating its full promote flow here), **Upstreams mini-table** (name/transport/strategy/enabled, up to 5 rows, "Manage" link routes to DNS Settings)), full range selector including live 1s polling, card add/remove/reorder with persistence (now 7 cards) | done for everything built (68 pre-existing Chromium checks in `chromium_smoke.mjs`, updated for the new 7-card count and scope-note text, plus 2 new dedicated checks for the Clients/Upstreams mini-panels; 2 new checks in `clients_access_smoke.mjs` proving the mini-panels show real, just-created client/upstream data end-to-end against a disposable instance -- 51/51 passing) | not started |

## DNS group

| Page | Route | Python API | Controls | Tables | Charts/live | Polling | States | Go API | Svelte | Browser test | Owner accepted |
|---|---|---|---|---|---|---|---|---|---|---|---|
| Query Log | `analytics` | `GET /api/analytics/query-log`, `/api/analytics/top-domains` | Filter form (window/search/domain/client/qtype/protocol/rcode/upstream/cache-status/limit/blocked-only); Refresh; **Query Log → rule creation (roadmap-required, not yet in Python V2)** | Recent Queries grid, Top Domains grid | — | none | degraded banner, active-filter badge, empty | **in progress** -- new `internal/rawquerylog`: a second, narrower Go->Python compatibility boundary, investigated and built rather than deferred (per the governing task's "a compatibility-boundary question is not an owner blocker" instruction). Python's own reader for this data (`app/v2/analytics_query.py`'s `PartitionPruningReader`) is DuckDB-backed, needing CGO -- the same already-documented reason `internal/pyanalytics`'s Top Blocked Domains was unavailable. Investigated further rather than assumed blocked: the underlying data is plain Parquet + Zstandard, so a genuine boundary is possible with a pure-Go reader (`github.com/parquet-go/parquet-go`, klauspost/compress for Zstandard, no cgo -- cross-compiles clean for both amd64 and arm64 under `CGO_ENABLED=0`, reconfirmed). Mirrors Python's own `enumerate_partition_files` partition-pruning exactly (same `YYYY/MM/DD/HH-*.parquet` layout, same year/month/day/hour-boundary pruning) and the same failure-isolation contract (one corrupt/unreadable segment -- including a real caught bug, `parquet-go` panicking rather than erroring on a bad magic header, now recovered -- never breaks the rest of a query). `GET /api/analytics/query-log` (exact-match filters on client/domain/qtype/protocol/rcode/upstream/cache_status/blocked, plus an optional post-scan substring `search`, matching Python's own contract) and `GET /api/analytics/top-domains`'s `blocked_only` path are both real when `-query-log-dir` is configured; honestly degraded (never silently empty) when it isn't -- same nil-reader contract as `internal/pyanalytics`. Proven against the actual live preview's real, actively-written query history, not just synthetic fixtures. **Disclosed trade-off** (in the package's own doc comment): unlike Python's DuckDB predicate pushdown, this reader filters after reading whole row groups into Go, bounded by an in-memory row-scan safety cap on a very wide time window -- correct for the common small-window "recent queries" case, but a genuinely large historical scan may hit the cap before finding every match. **Not built**: Query Log -> rule creation (needs Custom Rules' own further design, not just this boundary) | **in progress** -- `QueryLogView.svelte`: full filter form (window presets + search + all 7 allowlisted filters + blocked-only + limit), active-filter-count badge, degraded banner, DataGrid of results (time/domain/client/type/protocol/rcode/blocked badge/latency/cache/upstream). Discloses its own scope in-page. Top Domains grid is not duplicated here (already on Dashboard) | done for what's built (9 dedicated Chromium checks against a real seeded Parquet fixture -- real rows render, domain filter narrows server-side, active-filter badge appears, clearing filters restores the full grid -- plus the full viewport/theme sweep at `/ui/analytics`; 8 new `acceptance.py` checks against the same real fixture format, plus 2 checks proving the honest-degraded path when `-query-log-dir` isn't configured). Two real bugs the suite caught before shipping: (1) `parquet-go` panics rather than erroring on a corrupt segment -- recovered; (2) a test selector (`button[type="button"]`) ambiguously matched the *first* such button in the form (one of the window-preset buttons, not "Clear filters") -- fixed by giving Clear Filters its own class, same test-selector-ambiguity bug class already found twice elsewhere this session (`.restore-confirm input`) | not started |
| Clients | `clients` | `GET /api/clients`, `/api/discovery/observed-clients`, `/api/discovery/status`, `/api/groups`; `POST /api/clients`, `/clients/{id}/identifiers`, `/clients/{id}/identifiers/generate`, `/clients/{id}/identifiers/{iid}/{revoke,regenerate}`, `/clients/{id}/domain-overrides`, `/clients/{id}/groups`, `/discovery/observed-clients/{ip}/promote`; `DELETE /clients/{id}/identifiers/{iid}`, `/clients/{id}/domain-overrides/{oid}`, `/discovery/observed-clients/{ip}` | Metric strip; "Manage Client" (not "Promote" — locked owner decision) + Forget; Create Managed Client form; Explain link → Policies | Observed Clients grid, Managed Clients grid | — | none | unmanaged/client-N badges, empty | **in progress** -- `internal/clients` (new, native Go schema/CRUD, field-matched against `control_db.py`'s clients/client_identifiers/client_groups/client_group_members tables): create client, create group, add identifier (ipv4/ipv4_cidr/ipv6/ipv6_cidr, format-validated via `net/netip`), assign to group. Per-client policy assignment is done (see the "Clients & Access" row -- `internal/policy`, wired into this page's DataGrid). **Strong ClientID: DONE (2026-08-27)** -- real DoH/DoT/DoQ identity, not just a stored opaque string: `internal/clientid` (OS-backed CSPRNG generation, exact 48/64-lowercase-hex validation, full-entropy DoH path, lossless DNS-label-safe SNI encoding for DoT/DoQ), full lifecycle (`GenerateClientID`/`RevokeIdentifier`/`RegenerateIdentifier`/`DeleteIdentifier`, revoke sets a timestamp rather than deleting so history survives), per-client explicit domain block/allow overrides. Compiled into real dnsdist enforcement by `internal/dnscompile` (`HTTPPathRule`/`SNIRule`/`SetTagAction`/`TagRule`/`AndRule`, `addDOQLocal` for DoQ) ahead of the global blocklist/regex chain, giving real explicit-deny > explicit-allow > default precedence with structural isolation between clients -- verified against the real installed dnsdist 2.1.1 binary and, in `internal/hostagentd`, against real disposable `named`/`dnsdist` processes with real DoH (HTTP/2 POST)/DoT (raw TLS)/DoQ (QUIC handshake) client connections, including revoke/regenerate changing live enforcement and a real `SIGKILL`-and-re-promote restart-persistence proof. The existing DNS Runtime promote pipeline needed no changes (it already fully restarts dnsdist on every promotion). **Full managed-client lifecycle: DONE (2026-08-27)** -- `internal/clients` gained `UpdateClient` (name/description edit), `SetClientEnabled` (a disabled client's identities are excluded from `AllActiveClientIdentities`, so disabling is a real, live-enforcement-affecting action once applied, not just a display flag), `DeleteClient` (explicitly transactional: deletes `client_identifiers`/`client_group_members`/`client_domain_overrides` itself rather than relying on the schema's `ON DELETE CASCADE` annotations, which are inert everywhere in this codebase since nothing ever sets `PRAGMA foreign_keys=ON` -- a real, disclosed, separate finding, not fixed globally by this one method), and `RemoveFromGroup`. Wired to `PATCH /clients/{id}`, `POST /clients/{id}/enabled`, `DELETE /clients/{id}`, `DELETE /clients/{id}/groups/{groupId}`. **Observed Clients: a real, disclosed-narrower substitute, DONE (2026-08-27)** -- not the Python `observed_clients`-in-`control.db` boundary described below (still refused, still blocked on the same secrets-exposure gap), but `GET /api/clients/observed`, a genuinely live source: it reads the "client" dimension straight from `internal/pyanalytics`'s already-safe analytics snapshot boundary (never Python's `control.db`, so none of the credential-exposure concern applies), excludes loopback/unspecified addresses so a prefix/loopback is never presented as a host, and cross-references every observed address against already-managed ipv4/ipv6 identifiers so the UI can tell a genuinely new address apart from one that's already managed. Disclosed narrower than a real discovery worker: no first-seen/last-seen history, no hostname/vendor/OS fingerprinting -- real, live, truthful traffic data, not a placeholder. "Manage Client" now works against this real data (create-new-client-or-attach-to-existing + add the observed address as an identifier, both real mutations) | **DONE (2026-08-27)** -- `ClientsView.svelte`: add-client/add-group forms, DataGrid-backed client table with identifier/group chips, inline add-identifier/assign-group/policy forms per row (policy editor via the shared `PolicyEditor.svelte`). Strong ClientID UI: generate (bit-strength + label), per-identity DoH path/SNI hostname display with copy buttons, revoke/regenerate/delete (each confirmed), per-client domain override chips with inline add/remove. Full lifecycle UI added this session: Edit (name/description, persists), Enable/Disable (real badge), Delete (confirmed, cascades client-side view immediately), remove-from-group (chip-x), remove-IP-identifier (chip-x). Observed Clients grid: address/query-count/managed-or-not, "Manage Client" flow (new client or attach to an existing one), honest degraded/empty states, loopback/unspecified never shown as a host. Discloses its own scope (no whole-client access-rules subsystem -- see the Clients & Access row) in-page | done (13 Chromium checks pre-Strong-ClientID incl. full viewport/theme sweep, plus this session's dedicated `clients_access_smoke.mjs` -- 45/45 checks against a fresh instance: full Strong ClientID lifecycle incl. generate/DoH-path/SNI/regenerate-produces-a-different-value/revoke-shows-badge, domain overrides, group assign+remove, IP identifier add+validate, edit/enable/disable/delete, Observed Clients honest-empty-state, per-client policy editor + Explain panel. Two real test-selector races the run itself caught and fixed, not app bugs: an open `<select>`'s own `<option>` text satisfying a "did the chip appear" wait before the real chip rendered, and the DataGrid's own empty-row placeholder counting as a row after delete) | not started |
| Clients & Access (Policies) | `policies` | `GET /api/policy/global`, `/api/networks`, `/api/groups`, `/api/clients`; `PUT /policy/{global,network/{id},group/{id},client/{id}}`; `POST /networks`, `/groups`; `GET /policy/explain` | Global/Network/Group/Client policy editors (shared field set: filtering profile, safesearch, parental/security policy, blocking response mode+custom IP, upstream, fallback strategy, ECS mode, domain routing, query-log/statistics tri-state); Create Network/Group forms; Explain panel | expandable per-object editors | — | none | inherit badges | **in progress** -- `internal/policy` (new, native Go): the full 15-field `policy_layers` table + upsert/load, `policy_networks` CRUD, field-matched against `policy_store.py`/`policy_model.py`'s `PolicyLayer`. Global/network/group/client PUT all wired; `GET /api/clients` and `GET /api/groups` embed each object's own layer (matching Python). **"Effective policy explain" (2026-08-27, done)**: new `internal/policy/effective.go`, a direct field-for-field port of `policy_compiler.compile_effective_policy`/`policy_service.explain_policy_for_client` (read directly, not guessed). `MergeLayers` is the pure per-field "last non-nil layer wins, else the documented default" merge (all 15 fields, same defaults as `policy_model.py`'s `DEFAULTS`); `OrderGroups` is the same ascending-priority/group_id-tie-break ordering as `order_groups`; `MatchNetwork` is the same most-specific-CIDR-wins matching as `network_match.py`'s `CompiledNetworkTable.match` (via `net/netip`, real IPv4/IPv6-never-cross-matches proof). `GET /api/policy/explain?client_id=&client_ip=` wires it to real storage (`internal/policy.Load` + `internal/clients.GroupsForClient`, new exported wrapper). Still narrower than Python: no schedule-layer override (Go has no `policy_schedules` storage/worker at all yet), disclosed in the file's own doc comment. **Compiled-runtime update (2026-08-26)**: a real fake-success bug fixed here -- `PUT /policy/global` used to unconditionally respond `{"promoted": true, "binding_count": 0}` regardless of whether anything was ever compiled (a "Runtime / Control / UI Truth" P0 violation). Global-scope saves now auto-apply through the real compiler (the only scope `internal/dnscompile` reads today), reporting a real `dns_runtime` result. Network/group/client-scope saves now honestly report `attempted: false` with an explicit reason instead of a fabricated `promoted: true`, since the compiler doesn't read those scopes yet -- no per-network compiled-runtime effect exists (distinct from the *explain* resolution engine above, which is now real). **`GET /api/networks` now embeds each network's own policy layer (2026-08-27)**, matching the `/api/clients`/`/api/groups` convention -- it used to omit it, an inconsistency noted and fixed while building the real Clients & Access page | **DONE (2026-08-27), rebuilt from sparse to real (2026-08-28)** -- `ClientsAccessView.svelte` behind the `policies` nav item now has three real scope panels plus a standalone Explain workbench, matching the old Python "Policies / Explain" page's own information architecture (Global/Networks/Groups beside an Explain drawer) instead of the original two-panel, no-Groups, no-Explain page: Global Policy editor, Networks (create by CIDR + per-network policy, each in its own `.networks-list`), and **Groups (new)** -- a real list (name, priority, member count, live policy summary chips) with the same shared `PolicyEditor.svelte` used at every other scope. **Explain (new)**: a standalone client-picker + optional client-IP form driving the real `GET /api/policy/explain` resolution on its own, not only reachable per-row from the Clients table -- shows network match, group contributions, and the full field-by-field global->network->group->client table with color-coded source badges, plus a correct, honest empty state when no managed clients exist yet. Group MEMBERSHIP (assign/remove a client) and per-client policy/Explain deliberately stay on the Clients page next to the identity they act on -- disclosed in-page; this page owns each scope's policy CONTENT. **Deliberately not built, disclosed rather than hidden**: V1's separate whole-client `access_rules` subsystem (`app/clients.py`'s `add_access_rule`/`evaluate_access` -- a second, independent allow/deny engine keyed only by client IP). The real access-control mechanism this migration actually ships is the one already live in dnsdist: per-client explicit domain overrides (deny beats allow beats default policy, see the Clients page) layered under the same global->network->group->client policy precedence every other setting uses -- a second, separate whole-client-IP allow/deny engine on top of that would be real, disclosed duplication, not a missing feature | done for what's built -- `clients_access_smoke.mjs` (2026-08-28 pass, 59/59 across the full Clients + Clients & Access suite) covers this page fully: nav item works, page is not the "Coming Soon" placeholder, Global Policy editor renders/saves/persists across a full reload, creating a network adds a real entry to its own disambiguated list, its per-network policy editor opens, the Groups panel renders real (non-placeholder) content, and Explain is proven in BOTH real states -- the honest empty state with zero managed clients, and (after creating one) a populated run producing a real field-by-field table and summary line. Run against a disposable fresh instance per this suite's own standing rule, never the live owner appliance. Manual desktop/rail/mobile (1440/900/390px) viewport pass: no horizontal overflow, real single-column collapse under the page's 60rem breakpoint | not started |
| Local DNS | `localdns` | `GET /api/local-dns`, `POST /api/local-dns` | Add Record form (name/type/value/TTL); **edit + delete are a Milestone-1 addition already, ahead of Python V2** | Records grid | — | none | — | **done** (Milestone 1). **Compiled-runtime update:** every create/update/delete now also compiles and promotes the real Go DNS runtime (`internal/dnscompile` + `internal/dnsruntime` -- see the "DNS Runtime Compiler" section below) -- a saved record now actually answers real `dig` queries through a real dnsdist this deployment's own host-agent supervises, reported inline as a `dns_runtime` response field | **done** (Milestone 1) | not started | not started |
| DNS Settings (Upstreams & Routing) | `upstreams` | `GET /api/upstreams`, `/api/domain-routing`; `POST /upstreams`, `PUT /upstreams/{id}`, `POST /upstreams/{id}/{enable,disable}`, `DELETE /upstreams/{id}`, `POST /upstreams/reorder`; `POST /domain-routing` | Create/Edit Upstream form; per-row Edit/Enable-Disable/Delete + up/down reorder; last-enabled confirm guard (locked owner decision: zero managed upstreams allowed with warning); Domain Route form | Upstream Profiles grid (ordered), Domain Routes grid | — | none | native-recursion info banner when zero enabled | **in progress** -- `internal/upstreams`: native Go schema (`0003_upstreams.sql`) + full CRUD, field-for-field matched against `policy_store.py` (transport/strategy/endpoint validation including DoH's tls_hostname requirement, the last-enabled-managed-upstream confirm dance, reorder that never drops an omitted id). **Compiled-runtime update (2026-08-26, corrected):** the stale `runtime.promoted` unconditionally-`true` stub (`runtimeStub()`) was removed and every mutation (create/update/enable/disable/delete/reorder) now auto-applies through the real compiler via `applyDNSRuntimeBestEffort`, reported inline as a real `dns_runtime` field -- same auto-apply convention as Local DNS, not merely reachable through the manual "Apply Runtime Changes" action anymore (that action still exists for a bulk/manual re-apply, it's just no longer the only path). **Domain Routing (2026-08-27, done, closing the last real gap in this P0 row)**: new `internal/domainrouting` (native Go, `0010_domain_routing.sql`) -- exact/suffix rules, field-matched against `policy_store.py`'s `domain_routing_rules`, narrowed to one implicit global ruleset (disclosed in the package's own doc comment: Python's real per-network `ruleset_id` selection needs an effective-policy resolution engine that doesn't exist in Go yet). Compiled into `internal/dnscompile` (most-specific-suffix-wins, deterministic regardless of input order, terminal `PoolAction` per route, `QNameRule` for exact vs. `SuffixMatchNodeRule` for suffix -- validated against the real installed `dnsdist --check-config` binary in 5 new dnscompile tests). Auto-applies from day one (`POST`/`DELETE /api/domain-routing` both call `applyDNSRuntimeBestEffort`, no manual-Apply-only phase). Proven end-to-end with two distinct controlled fake upstreams (no internet-egress dependency): a query under the routed suffix gets the route's own answer, a query outside it still gets the default's -- proving the route is followed AND doesn't leak into unrelated traffic (`TestDNSRuntimePromoteDomainRoutingSendsMatchedDomainToItsOwnUpstream`), plus the same proof through the real HTTP `POST /api/domain-routing` handler itself (`TestDomainRouteCreateAutoAppliesAndRealAnswerChanges`) | **in progress** -- `UpstreamsView.svelte`: create/edit form (dynamic per-endpoint rows, transport-conditional fields), DataGrid-backed profile table with address chips, enable/disable/delete/reorder, and the last-enabled confirm dialog rendered inline per-row (not a native `confirm()`). **Domain Routing UI added (2026-08-27)**: a Domain Routing card (match-kind/domain/upstream-profile add form, a plain table of existing routes with Delete) below the profiles grid, sharing the same `DnsRuntimeBadge` feedback every other mutation on this page already uses | done for everything built (9 Chromium checks: create adds a real row, Disable on the sole enabled profile shows the confirm dialog rather than silently succeeding, confirming actually disables it, the native-recursion banner appears, full 5-route x 3-viewport x 2-theme sweep). Domain Routing UI has no dedicated Chromium coverage yet | not started |
| Cache | `cache` | `GET /api/cache/status`, `POST /api/cache/flush` | Per-BIND-context flush form (scope/target); dnsdist packet-cache flush; Refresh | plain status table | — | none | unavailable badge, empty | **done, via the new root-owned host-control agent**: the network-isolation blocker previously recorded here is real but was solved architecturally rather than by opening a port -- `apdns-hostagent` is a separate, root-owned daemon (`internal/hostagentd`) listening on a Unix socket, authorized per-connection by kernel-verified `SO_PEERCRED` (never anything the client asserts) against a single configured allowed UID (the web process's own), with a fixed compile-time allowlist of named operations -- no generic shell/command endpoint exists (proven directly: `exec`/`shell`/`run`/`command`/`eval`/`system.exec` are all rejected as unknown ops, same as any other made-up name). `cache.status`/`cache.flush` run real `rndc status`/`flush`/`flushname`/`flushtree` against the real BIND rndc port using the real preview's `rndc.conf` (argv-based exec only, never a shell string); dnsdist flush is explicitly denied with a message about the restart requirement rather than faked. **BIND Cache Counters (2026-08-27, done)**: `cache.status` also now returns each context's real hit/miss counters via a real, read-only GET of BIND's own statistics-channels JSON endpoint (`internal/hostagentd`'s `fetchBindCacheStats`, field-matched against `app/v2/cache_control.py`'s `bind_cache_stats`) -- no rndc/auth involved, the same trust model as Python's own implementation. Proven against a real, disposable `named` instance with statistics-channels enabled (not a synthetic HTTP server): real hits/misses/hit-ratio parsed correctly. Every request is recorded to an append-only JSON-lines audit log (`op`, real `peer_uid`, `ok`, duration). Proven end-to-end against a real disposable `named` instance, and separately against two genuinely different real UIDs (root running the agent, an unprivileged system user running the web binary) -- which caught and led to fixing a real bug (an 0600 socket file that blocked the legitimate client before authorization even ran) no same-UID in-process test could have found. **Cache correctness/isolation: proven, not just observed (2026-08-28)**: an architecture audit (read `internal/dnscompile.go` directly, not assumed) found the design is already leak-safe by construction -- Strong ClientID identity/deny/allow rules, Local DNS records, rewrite rules, and the global blocklist are ALL compiled as terminal rules (`SpoofAction`/`AllowAction`/`PoolAction`, every one of which stops further rule processing) that run BEFORE the one dnsdist packet cache (`newPacketCache`, attached only to the unnamed default pool) is ever reached -- a query that would be blocked, spoofed, or routed elsewhere never touches the cache in either direction. Separately, every `dns_runtime.promote` fully restarts the dnsdist process (`reloadDnsdist`'s `stopDnsdist`+fresh start, not an in-place reload), so `newPacketCache()` is always a brand-new, empty cache after any promotion -- a newly-applied block/allow/routing change can never be bypassed by a stale cached answer from before the change. Both claims are now proven with real disposable `named`+`dnsdist` fixtures, not just read from the code: `TestPacketCacheNeverServesAnAllowedClientsAnswerToABlockedContext` (a Strong-ClientID client with an explicit per-domain ALLOW override populates the shared default-pool cache with a real counted upstream answer; an ordinary untagged query for the same domain, right after, gets REFUSED and the real upstream is never asked on its behalf -- proving the block short-circuits before the cache, not just that the two answers happen to differ) and `TestPacketCacheIsClearedOnEveryPromote` (warm the cache with a real answer, promote again with the same domain now blocked, prove the second query is REFUSED, not the stale cached NOERROR). A real, unrelated test-infrastructure bug was found and fixed while building these: dnsdist forwards a TCP-received query (DoT/DoH/plain TCP) to its backend over TCP too, not UDP -- the existing UDP-only fake-upstream test helper silently failed for any DoT/DoH test that actually reaches a live backend (every existing identity test terminates earlier via a Local DNS `SpoofAction`, so this never surfaced before); fixed by giving the new counting fake upstream a real TCP listener too. **Not attempted this session, explicitly out of scope by design**: no ECS (client-subnet) support was added -- correctly treated as its own separate privacy/cache-key/capacity decision, not something to add casually while proving isolation of what already exists. Duplicate-in-flight-request deduplication (singleflight) was not added either: no measurement in this session's real fixtures showed BIND/dnsdist failing to coalesce concurrent identical lookups on their own, so none was added speculatively | done | done | done (`hostagent_smoke.mjs`, shared suite across all 5 host-agent-backed areas -- real BIND-context render for this page specifically, plus a new check for the real Cache hits/misses column; 5 new Go tests for `fetchBindCacheStats`, including two against a real disposable `named` instance with statistics-channels enabled; **2 new real cache-isolation tests, 2026-08-28**, see above) |

## Security group

| Page | Route | Python API | Controls | Tables | Go API | Svelte | Browser test | Owner accepted |
|---|---|---|---|---|---|---|---|---|
| Filters (Custom Rules) | `filtering` | `GET /api/services`, `/api/service-rulesets`, `/api/schedules`, `/api/policy/global`; `PUT /policy/global`; `POST /services`, `/service-rulesets`, `/schedules`; `PUT /policy/schedule/{id}` | Global Answer Policy editor; Create Service/Ruleset/Schedule forms; **V1/AdGuard-style allow/block/regex/rewrite/precedence + Query Log→rule (locked owner decision, not yet in Python V2 — required for both)** | Service Catalog, Rulesets, Schedules grids | **in progress** -- `internal/customrules` (new, native Go): block/allow/regex_block/regex_allow/rewrite rule types, precedence via a `priority` column + reorder, bulk enable/disable/delete. This is new functionality per the roadmap's locked "Custom Rules" decision (Python V2 has no custom-rules API at all -- only V1's free-text AdGuard-syntax parser, `app/custom_rules.py`), so it's a *structured builder*, matching the roadmap's explicit "may provide a modern rule builder" allowance, not a port. **Not built**: free-text V1/AdGuard-syntax import (a real ~1000-line parser port, not a trivial addition), Query Log -> rule (needs Query Log, which needs the Parquet/DuckDB path -- see `internal/pyanalytics`), Service Catalog/Rulesets/Schedules (service-based blocking, separate from custom rules), rule test/evaluate preview. **Compiled-runtime update (corrected 2026-08-27 -- this line was stale, verified against the actual code, not re-done)**: block/allow/regex_block/regex_allow/rewrite rule types compile into the real dnsdist runtime (`internal/dnscompile`) AND every mutation handler (create/update/toggle/reorder/delete/bulk) already calls `applyDNSRuntimeBestEffort` -- `internal/httpapi/handlers_customrules.go` (confirmed by direct code read, not the stale prose that used to be here), same auto-apply convention as Local DNS/Upstreams/Domain Routing | **in progress** -- `CustomRulesView.svelte`: add/edit/toggle/delete, bulk select+enable/disable/delete, up/down reorder, plus the shared Global Answer Policy editor (`PolicyEditor.svelte`, same component as Clients & Access). Discloses its scope in-page | done for what's built (7 Chromium checks: add block + rewrite rules, rewrite target displays inline, bulk-disable actually disables both selected rows, full viewport/theme sweep) | not started |
| Blocklists | `blocklists` | `GET /api/blocklists`; `POST /blocklists/settings`, `POST /blocklists`, `POST /blocklists/{id}/interval`, `PATCH /blocklists/{id}`, `POST /blocklists/{id}/toggle`, `DELETE /blocklists/{id}`, `POST /blocklists/{id}/refresh`, `POST /blocklists/refresh-all`, `GET /blocklists/jobs/{id}` | Update All; global interval + Save; Add Subscription; per-row Edit (dialog)/Update Now/Enable-Disable/Delete; attention card (≥3 consecutive failures) | Subscriptions grid, Recent Updates | **done** (Milestone 1, incl. immediate pull, attention threshold, previous-good retention, per-row pending feedback, stale-delete guard). **Compiled-runtime update:** each enabled subscription's already-promoted `.rpz`-style runtime file (`<id>.rpz`, real domains, previously staged but not consumed by any live resolver) is now read by `internal/dnsruntime` and compiled into the real dnsdist runtime via the DNS Runtime page's manual "Apply Runtime Changes" action -- see the "DNS Runtime Compiler" section below | **done** (Milestone 1) | in progress (Chromium sweep covers grid rendering + a sortable header on every viewport/theme; full lifecycle -- Edit dialog, Update Now, attention-card recovery -- proven in Milestone 1's own acceptance.py but not yet re-asserted through this shell's Chromium harness) | not started |
| Encryption | `encryption` | `GET /api/tls/status`, `/api/dns-transports`; `PUT /dns-transports`; `POST /tls/replace`; `GET /dns-transports/mobileconfig/{protocol}`, `POST /dns-transports/dnscrypt/rotate` | Cert status + replace form; DoT/DoH/DoQ/DoH3/**DNSCrypt** (locked owner decision: first-class, discoverable, not hidden) enable+port form; Apple `.mobileconfig` download links | TLS status table | **in progress** -- two independently-scoped native pieces. (1) `internal/dnstransports`: real native-Go storage for DoT/DoH/DoQ/DoH3/DNSCrypt enable+port(+doh_path/provider_name), matching `app/v2/policy_store.py`'s `dns_transport_settings`/`dnscrypt_settings` field-for-field for what it stores. **Compiled-runtime update:** DoT and DoH settings now compile into the real dnsdist runtime (`addTLSLocal`/`addDOHLocal`, reusing this appliance's own management TLS cert/key, matching Python's real `DotConfig`/`DohConfig` cert-reuse pattern) and auto-apply on save. DoQ/DoH3 are not compiled yet (each needs its own listener wiring this pass didn't have scope for). DNSCrypt here is enable/port/provider_name only -- no identity/certificate provisioning, since real DNSCrypt identity generation is a separate, real piece of work this pass didn't build (`dnscrypt_identity_provisioned` is honestly always `false`). (2) `internal/tlscert`: **real ownership change** -- this now manages this control plane's OWN real management TLS certificate (the same one its HTTPS listener serves, and DoT/DoH reuse), not a read-only mirror of Python's. Real upload/replace (`POST /api/tls/replace`, field-matched against Python's own `app/v2/tls_cert.py`, read directly): parses both PEM blobs, confirms the key's public component matches the certificate's, checks the validity window, and only promotes atomically on success -- a bad replacement never touches the working certificate (proven by a real test that stages a bad replacement and asserts the original file is byte-identical afterward). Matches Python's own real, disclosed `restart_required: true` contract exactly (a process-level TLS listener does not hot-reload; Python's own comment calls this "standard behavior... not a defect," and this implementation makes the identical choice rather than building a hot-reload path the reference doesn't have either). **`.mobileconfig` generation: done (2026-08-27)** -- new `internal/mobileconfig`, field-matched against `app/v2/mobileconfig.py` (read directly): the same Apple `com.apple.dnsSettings.managed` profile shape (a purpose-built minimal XML plist writer, not a generic library -- deterministic key order, no new dependency), built entirely from this control plane's own already-native state (`internal/dnstransports` + `internal/tlscert`, no new compatibility boundary, no new mount). Same real validation as Python: refuses with an actionable error if the requested transport isn't enabled or no certificate is active yet, matching Python's own `MobileconfigError` messages. `GET /api/dns-transports/mobileconfig/{protocol}` wired for `dot`/`doh` (the only two Apple's format supports, same as Python). **Not built**: DNSCrypt key rotation (blocked on DNSCrypt identity generation itself not existing yet) | **done for what's built** -- `EncryptionView.svelte`: TLS status card (real cert subject/validity/SAN/self-signed), a real certificate+key file upload/replace form reporting the real promoted subject and the restart-required notice, DNS Transports form (all 5 protocols' enable+port, DoH path, DNSCrypt provider name), discloses its own remaining scope in-page. **`.mobileconfig` download links added (2026-08-27)**: a real link per enabled transport (DoT/DoH), absent when that transport isn't enabled yet | done for what's built (11 Go tests for `internal/tlscert`'s write side + 2 real httpapi round-trip tests, incl. a real mismatched-pair-never-touches-live-files proof; 5 pre-existing Chromium checks -- TLS status renders a real cert subject, saving DoT settings round-trips through a real save and survives a full page reload -- plus the full viewport/theme sweep at `/ui/encryption`). A real bug the Chromium suite caught before shipping: triple-click-to-select-all is unreliable on a `<input type="number">` in headless Chromium, leaving the old value in place so typed digits appended instead of replacing it -- produced an out-of-range value that silently failed the input's `max` constraint and blocked native form submission with no visible error; fixed by clearing the value directly instead of relying on triple-click selection. **New this session**: 6 `internal/mobileconfig` unit tests (well-formed-XML proof, DoH/DoT field shapes, all 3 real refusal cases, first-SAN-entry-used-as-hostname), 3 `internal/httpapi` real round-trip tests, and a dedicated real-Chromium run against a disposable instance with a real self-signed cert provisioned at startup: no download link before enabling DoT, link appears after a real save, downloaded profile is a real, well-formed plist referencing the real cert's own hostname -- 9/9 checks passing (script not committed -- ad hoc verification, cleaned up after the run) | not started |

## Operations group

| Page | Route | Python API | Controls | Tables | Go API | Svelte | Browser test | Owner accepted |
|---|---|---|---|---|---|---|---|---|
| Import | `importexport` | `GET /api/import/jobs`; `POST /import/jobs`, `/import/jobs/{id}/apply`; `GET /api/migration/detect` | Source-type select (AdGuard YAML/live API, Pi-hole paste, hosts, BIND zone, Alderpoint CSV/XLSX/JSON); preview with per-item selection + status badges; Apply | Recent Import Jobs grid | **in progress** -- new `internal/importer`: one real, native source type (hosts file), matching `app/importer.py`'s `parse_hosts_text` semantics (`#` comments, multiple hostnames per line, malformed lines skipped not fatal) and writing straight into the existing `internal/localdns` store -- no new schema. Simplified from Python's version: no `default_domain` suffixing for bare hostnames. **Not built, disclosed rather than hidden**: the other five source types (AdGuard YAML/live API, Pi-hole paste, BIND zone, Alderpoint CSV/XLSX/JSON) -- `app/importer.py` is ~1,200 lines covering all of them plus a migration-plan/preview system with per-item selection and status badges, a substantial undertaking each, not attempted here; no async Import Jobs (this import is synchronous, returning imported/skipped counts + per-line errors directly, not queued) | **in progress** -- `ImportView.svelte`: a hosts-file textarea + Import button, real imported/skipped counts and per-line error list, discloses the other 5 source types as not built in-page. No source-type select, no preview/per-item selection, no Jobs grid | done for what's built (4 dedicated Chromium checks: add a real hosts-file import, see the real imported count, confirm the record actually landed on Local DNS -- plus the full viewport/theme sweep at `/ui/importexport`; 3 new `acceptance.py` checks incl. a malformed line being skipped without aborting the rest) | not started |
| Backup & Restore | `backup` | `GET /api/backup/appliance`, `/api/backup/secrets`; `POST /backup/appliance{,/upload,/upload-settings}`, `DELETE /backup/appliance/uploads/{id}`, `POST /backup/appliance/{name}/{validate,restore}`, `POST /backup/secrets{,/…/validate,/…/restore}` | Create local/portable backup; upload+preview dropzone (`.apdnsbak`/`.tar.gz` — **V1.1.1 `.tar.gz` compatibility is a hard requirement**); retention settings; restore preview (recommended/all/none selection, conflict policy, sensitive-category ack, typed-filename confirm — **mandatory pre-restore safety backup is a locked requirement**) | Appliance backups, Uploaded archives, Safety backups, Restore jobs, Secret backups grids | **in progress** -- `internal/backup` (new, native Go): `VACUUM INTO` snapshots of every native-Go-owned table (blocklists, Local DNS, DNS Settings/upstreams, Clients & groups, Filters/custom rules, Clients & Access policy layers/networks -- deliberately excludes `sessions` and `schema_migrations`) into a manifest-tagged tar archive, grouped into 7 real restore categories (`admin_accounts`, `blocklists`, `local_dns`, `upstreams`, `clients`, `policy`, `custom_rules`, exposed via `GET /api/backup/categories`); list/create/upload/preview/delete. The manifest carries real structured contents (a per-table row count, not an opaque blob label -- a genuine "preview before you restore"). Restore accepts an optional category list: given none, it's the historical all-or-nothing restore; given some, only those categories' tables are touched and every other table's live data is provably left alone (proven by a test that changes two categories, restores only one, and asserts the other kept its post-backup value). Restore is genuinely transactional either way (`ATTACH DATABASE` + a single transaction) and always takes a mandatory pre-restore safety backup first (itself a *full* backup regardless of the restore's own scope), listed like any other backup so nothing is silently discarded. Retention (`-backup-retention-max-count`/`-backup-retention-max-age-days`, both opt-in) prunes old *manual* backups after each create -- the safety backup is never auto-pruned, only ever deleted by an explicit operator action. Archive-bomb defenses: a compressed-size cap (500MB) and an expanded-size cap (2GB), both enforced before any extraction happens. **Not built, and explicitly disclosed in the format's own doc comment**: no byte-compatibility with Python's encrypted `.apdnsbak` (this is a separate container/manifest format with no shared crypto -- a real bridge to either legacy format is future work, not attempted here; investigated at ~5,000 lines across `app/backup.py`/`app/v2/backup_restore.py`/`backup_selective.py`/`migration.py`, a genuine V1-schema-to-V2 data-migration project of its own, not a rushed add-on), no V1.1.1 `.tar.gz` import (same investigation, same conclusion), no secrets/certs in the archive (this control plane doesn't own any secrets store yet), no scheduled/automatic backups (retention prunes what exists, it doesn't create new ones on a schedule), no Secret Backups grid (`/api/backup/secrets` not built), no conflict-policy options, no async Restore Jobs history (restore is synchronous, not queued) | **in progress** -- `BackupView.svelte`: create/upload cards, a DataGrid of stored backups (filename/created/reason/size), a per-row restore dialog with a real per-category picker (defaults to every category selected/full restore; unchecking down to zero re-disables the confirm button; each category shows its row count from the manifest's structured preview) plus the typed-filename confirmation (Confirm restore stays disabled until the exact filename is typed -- same locked-confirm pattern as Upstreams' last-enabled-profile guard), delete. Discloses the `.apdnsbak`/`.tar.gz` gap in-page. No conflict-policy options, no Secret Backups grid, no Restore Jobs grid | done for what's built (12 dedicated Chromium checks -- create adds a real row, restore-confirm is correctly gated on an exact filename match *and* on at least one category being selected at every stage, restoring reports the automatic safety backup by name and that backup itself appears in the list afterward -- plus the full viewport/theme sweep at `/ui/backup`; 19 `acceptance.py` checks incl. a real file-upload round trip, corrupt-archive rejection, structured per-table counts in the manifest, the categories endpoint, a selective restore that changes two categories and proves only the selected one rolled back, rejection of an unknown category name, and a full restore actually changing data back with a safety backup taken first). A real bug the Chromium suite caught before shipping: the category checkboxes and the filename-confirmation input are both `<input>` elements in the same dialog, and a test selector written before the picker existed (`.restore-confirm input`) silently started matching the first checkbox instead of the filename field once the picker was added -- fixed by giving the filename input its own class and a selector that names it explicitly, not just by making the test pass | not started |
| Replication | `replication` | `GET /api/node-identity`, `/api/replication/{health,peers}`; `PUT /replication/peers/{id}`; `DELETE /replication/peers/{id}`; `POST /replication/peers/{id}/sync`, `/replication/issue-peer-cert` | Metric strip; Add/Update Peer form (identity, URL, cert SHA-256, CA/client cert/key PEM, direction); per-peer Sync | Peers grid | **status read path done via the host-agent, mutating paths remain out of scope for the reasons found**: of the three original blockers, (1) is now solved the same way as Cache -- `apdns-hostagent` opens `control.db` itself, root-owned, read-only (`mode=ro`, `query_only(1)` pragmas), and returns only the same redacted fields Python's own `Peer.public_dict()` exposes; `ca_pem`/`client_cert_pem`/`client_key_pem` are never read into the response (proven directly: a test seeds real secret-shaped strings into those columns and asserts none of them ever appear in the JSON-encoded result, both against a synthetic fixture and against the actual live `control.db`). (2) and (3) are unchanged and still real: issuing a peer cert still needs Python's protected SecretStore-held CA key (no Go-native secrets store exists), and full mTLS push/pull sync is still Python's own bespoke protocol with no second real peer in this environment to test a Go implementation against. `replication.sync` therefore does a real, narrow, bounded HTTPS reachability check against the peer's configured URL validated against the peer's configured CA -- explicitly disclosed as connectivity-only, not Python's actual push/pull. Peer add/update/delete/cert-issuance remain not started | in progress (status/identity real; peer mutation and full sync protocol not started) | in progress (status page only) | done for what's built (`hostagent_smoke.mjs`: real node identity from the live `control.db` renders, confirmed non-secret) |

## System group

| Page | Route | Python API | Controls | Tables/panels | Go API | Svelte | Browser test | Owner accepted |
|---|---|---|---|---|---|---|---|---|
| Statistics | `statistics` | `GET /api/statistics/export`; `POST /statistics/clear` | Export link; Clear form (typed "CLEAR" confirm + raw-history option) | — | **done (2026-08-27), including Clear** -- `internal/pyanalytics.ExportAll`: a real, unfiltered dump of `time_buckets`/`dimension_counts`, proven against the live preview's real aggregates.db (3,112 real bucket rows, 167,012 real dimension-count rows exported end-to-end). Matches Python's own export shape (raw per-query history deliberately excluded, same reasoning: already exportable via Query Log). **Clear: no longer blocked** -- re-investigated per this session's own instruction rather than left as a stale "deliberately not built" disclosure: a real `DELETE` against Python's live `aggregates.db`/Parquet tree, but run from `apdns-hostagent` (`internal/hostagentd/ops_analyticsclear.go`, new `analytics.clear` op) -- the same already-root process that already has real, unrestricted host-path access to that exact file for the analytics-snapshot publisher, so this needed no new read-write mount into the unprivileged web container at all, following the same Cache/Replication/Network-Configuration host-control-boundary precedent this row's own prior note suggested checking. A normal SQLite write connection (busy_timeout, no `immutable=1`), not the read-side hack that caused the earlier real corruption incident -- concurrent access from a second process is already Python's own accepted design (its writer worker and webapp already do this independently). `POST /api/statistics/clear` requires the same server-side "type CLEAR to confirm" check Python's own route enforces (never client-side-only), matches Python's `include_raw_history` default (true) and field shape exactly. **Analytics writer/receiver liveness (2026-08-26, owner-mandated acceptance rule)**: `internal/pyanalytics/health.go`'s `Reader.Health` closes the exact V1.1.1 failure class where DNS kept running while the analytics writer died on SQLITE_BUSY and the Dashboard/Top Domains kept showing a convincing all-zeros chart -- see the System Status row below and this package's own doc comment. Surfaced in `GET /api/health`'s new `analytics` component, the Dashboard summary's `analytics_health`, and `degraded=true` on `/api/analytics/{timeseries,live-activity,top-domains}` even when the SQL read itself succeeds | **done** -- `StatisticsView.svelte`: a real download link (`<a href="/api/statistics/export">`), plus a real Clear form (raw-history checkbox, typed-"CLEAR" confirm gating the submit button, real success/error reporting with exact cleared counts, matching the typed-confirm pattern already used by Backup & Restore/Upstreams) | done for what's built (3 pre-existing Chromium checks: nav/render/export-link-target, plus the full viewport/theme sweep at `/ui/statistics`; 1 `acceptance.py` check verifying the real exported shape; **new this session**: 5 Go tests (`internal/hostagentd/ops_analyticsclear_test.go`, `internal/httpapi/handlers_statistics_test.go`) including a real end-to-end proof through an actual unix-socket hostagent connection and a real SQLite file, plus a dedicated real-Chromium run against a disposable two-process (hostagent + web, real two-UID privilege separation) instance seeded with real rows: confirm-gating (disabled until exact "CLEAR" typed), and Clear reporting the real seeded counts back (10 buckets/10 dimension rows) -- 7/7 checks passing) | not started |
| System Status | `health` | `GET /api/health`, `/api/system/status`, `/api/node-identity`, `/api/discovery/status`, `/api/dns/performance`; `POST /dns/performance/benchmark`; `DELETE /dns/performance` | Copy UI Perf Report / Clear Measurements / Refresh; Run Safe DNS Benchmark / Copy DNS Perf Report / Clear | Metric strip, UI Performance table (session-only), DNS Performance panel, BIND Cache Counters, Components, Node Identity | **in progress** -- Metric strip and Components use the existing native `GET /api/health`/`/api/system/status` (no new backend needed). UI Performance is real, not synthetic: `perfLog.svelte.ts` records actual navigate()-to-component-ready latency for every route change (wired into `router.svelte.ts`/`RouteLoader.svelte`), session-only (an in-memory, capped-at-200-entries log; a reload starts empty, matching Python's own "session-only" semantics). **Node Identity: done (2026-08-27)** -- corrected a stale disclosure: `GET /api/replication/status` (real, root-owned, redacted `control.db` read via `internal/hostagentd`'s Replication ops, built in a prior session for the Replication page) already returns `node_identity` in its response; this page simply was never wired to fetch it. No new backend needed -- `SystemStatusView.svelte` now calls the same `api.replicationStatus()` the Replication page uses and renders a Node Identity card, honestly degraded (not fatal to the rest of the page) when the host-control agent isn't reachable. **BIND Cache Counters: also done (2026-08-27)** -- reuses the exact same `GET /api/cache/status` the Cache page already calls (now carrying real `cache_stats` per context, see the Cache row), rendered here as its own dedicated card. No new backend surface needed. **DNS Performance benchmark: done (2026-08-28)** -- new `internal/dnsperf`, field-matched against `app/v2/dns_performance.py`: real UDP/TCP/DoT/DoH packet-level query exchange (`QueryOnce`/`QueryManyEstablished`, including Python's established-connection-vs-per-query-setup latency distinction) and the same ceil-indexed percentile/summary math. The actual query exchange runs inside `apdns-hostagent` (`dns_perf.benchmark` op, `internal/hostagentd/ops_dnsperf.go`) because the web container's isolated bridge network cannot reach 127.0.0.1:<dnsdist/BIND port> on the host at all (same reasoning as Cache/Replication/Network Configuration). Safety design: the web layer sends only domain/qtype/protocol/queries/timeout/path per case, never a bare server -- the op resolves `dnsdist`/`bind_plain` targets from its own fixed startup config and keeps `dot`/`doh` targets loopback-only with a bounded port, so this op can never be redirected at an arbitrary host; query count and timeout are capped server-side. The case list (`internal/httpapi/dnsperf_build.go`) is built from this deployment's own live state (`internal/localdns`, `internal/blocklists`' compiled RPZ output, `internal/dnstransports`' enabled transports) and targets the real Go-managed dnsdist/BIND runtime only -- never Python's separate :8443 runtime. Along the way, corrected a stale `internal/dnstransports` doc comment: DoT/DoH/DoQ settings are already compiled into the real dnsdist runtime (see the Encryption row), now proven end-to-end by this benchmark's own DoT/DoH cases. **Still not built, disclosed rather than hidden**: Discovery status needs a compatibility boundary this migration hasn't built. **Analytics Health (2026-08-26)**: new, real -- the Components table's `analytics` entry and a dedicated Analytics Health card report writer status/staleness/tick count/last success/last error (from Python's already-existing `worker-heartbeats/analytics-worker.json`, zero Python changes), real inbox queue depth, and last committed bucket, giving a 3-tier ok/degraded/failed status that demotes overall `/api/health` status on a dead/stale writer -- see `internal/pyanalytics/health.go` and the Statistics row above | **in progress** -- `SystemStatusView.svelte`: real metric strip (appliance/version/uptime/status/timezone), real Components table, real UI Performance table with working Refresh/Copy/Clear, real Analytics Health card, real Node Identity card, real BIND Cache Counters card, **real DNS Performance card (2026-08-28)**: Run Safe DNS Benchmark / Copy DNS Performance Report / Clear Benchmark Measurements, a real per-case results table (count/success/timeouts/errors/p50/p95/p99/max), honest degraded/unavailable states throughout (no service wired, benchmark-run failure, corrupt/unreadable stored report). Discloses the rest of its own scope (Discovery status) in-page | done for what's built (5 pre-existing Chromium checks: nav/render, a real component status renders, the UI Performance table actually accumulates real per-navigation entries across the whole run and Clear Measurements actually empties it -- plus the full viewport/theme sweep at `/ui/health`; `hostagent_smoke.mjs` checks proving System Status's Node Identity card and BIND Cache Counters card render real data). A real infrastructure fix found while adding a prior session's row of tests, not a product bug: this suite's single Chromium tab had grown to 150+ full navigations in one run, and started intermittently hitting genuine Chromium renderer resource exhaustion (`net::ERR_INSUFFICIENT_RESOURCES` / navigation timeouts) near the end of a run -- confirmed not a memory/fd/disk shortage on the host by direct inspection each time, and reproduced 3 times running to different, unrelated routes, which ruled out a single flaky page. Fixed by recycling the Chromium page every viewport block in the sweep (8 navigations each) instead of one tab accumulating the whole run; the new page shares the same browser context so login cookies and the localStorage-persisted theme choice carry over with no re-auth or re-toggle needed. **DNS Performance proof (2026-08-28)**: `internal/dnsperf` unit tests exchange real UDP/TCP/DoT/DoH packets against disposable fixtures (a real generated self-signed cert for DoT/DoH, a real TCP-framed responder, a real HTTPS DoH server); `internal/hostagentd/ops_dnsperf_test.go` proves the op end-to-end through an actual unix-socket hostagent connection against a real UDP responder, plus the safety-boundary rejections (unknown target, excessive queries, out-of-range port, empty domain); `internal/httpapi/handlers_dnsperf_test.go` proves the full HTTP-to-hostagent-to-UDP-fixture round trip including the Start/poll/Clear lifecycle. Additionally verified against the real compiled `alderpointdns-go`/`apdns-hostagent` binaries in a disposable two-UID fixture (hostagent as root with `-allowed-uid 999`, web running as the real unprivileged `apdns-browsertest` UID, a real UDP responder standing in for dnsdist) driven by real `curl` HTTP calls through setup/login/CSRF -- a real 5-case, 10,000-query benchmark completed in 3.4s with 100% success and real non-zero p50/p95/p99/max latencies, then a real Clear removed the stored report. No Chromium run this session (the curl-driven real-binary proof above covers the same real backend path; a Chromium click-through of the new card is recommended for a following session to add to `hostagent_smoke.mjs`) | not started |
| Administration | `administration` | `GET /api/system/status`; `POST /session/password`, `/session/revoke-others` | Change Password; Revoke Other Sessions; **Timestamp Display selector (Browser Local / Appliance Time / UTC)** | — | done | done | done (Chromium, see below) | not started |
| Network Configuration | `network` | `GET /api/network/status`; `POST /network/apply`, `/network/confirm` | Change config form (IPv4/IPv6 mode+address+gateway); pending-config confirm with ~120s auto-revert | Current Settings table | **done, via the host-agent, with the same safety net as Python**: the isolation blocker previously recorded here is solved the same way as Cache/Replication -- the NET_ADMIN-equivalent access lives only in the root-owned `apdns-hostagent` process, invoked only through named, validated operations (`network.status`/`.apply`/`.confirm`/`.rollback`), never a generic command path. `network.apply` snapshots the current interface state, applies the requested addresses/gateway via explicit argv-only `ip` calls (never a shell string), and starts the same kind of ~120s auto-revert watchdog Python's own `network_config.py` uses -- unless `network.confirm` is called first, the change reverts automatically. `network.rollback` restores immediately on request. All four proven against real, disposable kernel network interfaces (veth pairs), including a genuine timer-driven auto-revert and a second-pending-change rejection | done | done | done for what's built (`hostagent_smoke.mjs`: real loopback interface status renders) |
| Notifications | `notifications` | `GET /api/notifications`; `POST /notifications`; `PUT`/`DELETE /notifications/{id}/secret`; `POST /notifications/{id}/test` | Create Provider (webhook/email_smtp/pushover/slack), Set/Replace/Revoke credential, Send Test | Providers grid | **Credentials: done (2026-08-28)**, closing this row's real gap -- `internal/notifications` now stores a provider's actual credential (a webhook/Slack URL, a Pushover `user_key:app_token` pair, or an SMTP password -- `app/notifications.py`'s own doc comment, read directly, is what identifies these as the sensitive half, not a separate "token" field) via the new native Go secrets subsystem (`internal/secretstore` + `apdns-hostagent`'s AES-256-GCM engine, see the new cross-cutting "Secrets subsystem" note below this table). `has_secret` is the only thing any API response ever says about it -- no plaintext read-back, ever. `Test` performs a REAL send (HTTP POST for webhook/Slack/Pushover, a real SMTP session with STARTTLS for email_smtp) entirely inside `apdns-hostagent`; this process never holds the decrypted value. Schema reworked (`0013_notification_secrets.sql`, verified zero existing rows on the live deployment before the destructive `DROP COLUMN endpoint`): `config_json` for email_smtp's non-secret host/port/from/to/username, `has_secret` replacing the old plaintext `endpoint` column outright. **Deliberately not built in this pass, disclosed rather than hidden**: the event-driven dispatch engine (`app/notifications.py`'s `EVENT_CATEGORIES`/subscriptions/dispatch/history/cooldown logic) and `app/notify_check.py`'s real health-condition checkers -- a provider configured here can genuinely receive a message today via Send Test, but nothing yet decides WHEN to notify automatically; a separate, larger workflow | **done (2026-08-28)** -- `NotificationsView.svelte`: kind-aware create form (SMTP host/port/from/to fields appear only for email_smtp), a credential input on create, per-row Set/Replace/Revoke credential and Send Test actions with real inline pass/fail detail, discloses the dispatch-engine gap in-page | `internal/secretstore` (9 tests) + `internal/hostagentd/ops_secrets*.go` (15 tests, incl. real HTTP webhook exchange, real fake-SMTP-server session, wrong-kind/wrong-owner_ref/tampered-ciphertext/unknown-key-version rejections, master-key restart-persistence) + `internal/notifications` (18 tests, incl. a real end-to-end SetSecret→Test→real-disposable-webhook-server round trip and delete-revokes-secret) all pass against real fixtures, no mocks at the crypto or network layer. **Chromium: done (2026-08-28)** -- `notifications_smoke.mjs`, a disposable web+apdns-hostagent pair (secrets require a real hostagent) plus a throwaway local HTTP receiver standing in for a webhook target: 11/11 checks, including the receiver's own log actually getting a real, well-formed POST body from apdns-hostagent (not just a green checkmark in the UI), and Send Test correctly disabling again after Revoke | not started |

**Secrets subsystem (new, cross-cutting, 2026-08-28)**: `internal/secretstore` (web-side: ciphertext + metadata storage, calls into apdns-hostagent, never holds plaintext) + `internal/hostagentd/ops_secrets.go` (root-only half: AES-256-GCM master key, generated with `crypto/rand` on first use, persisted at `<KeyDir>/master.key.v1` mode `0600` inside a `0700` directory, never copied into the web container). `OpSecretsSeal` is the only place a secret's plaintext ever crosses the hostagent socket, and only inward; every USE of a stored secret is a narrow, named, purpose-specific op (`OpSecretsNotifyTest` today) that decrypts internally and performs exactly one bounded real action -- there is no generic decrypt endpoint anywhere in this system. Context binding: AEAD associated data is `kind|owner_ref`, so ciphertext copied to a different record/kind can never decrypt (proven by dedicated tests). This is the foundation the Notifications row above is built on; Replication (peer cert/CA key), authenticated Upstreams (endpoint `secret_ref`), and DNSCrypt (identity private key) are the next real consumers, not yet wired -- see AGENT_PROGRESS.md's decision-grade full-disk-encryption note for what this subsystem is deliberately NOT (SQLCipher/app.db encryption remains a separate, undecided project).
| Software Updates | `updates` | `GET /api/updates/{status,jobs}`; `PUT /updates/settings`; `POST /updates/upload`, `/updates/jobs/{id}/apply` | Metric strip; Private Feed form; Manual `.deb` upload+stage; per-job Apply (with reconnect-poll after restart) | Update Jobs grid | **done, but deliberately scoped to this control plane's own binary, not Python's dpkg package**: reimplementing arbitrary `.deb` install with root package-management access was refused as too destructive-capable for an unverifiable first cut (a bad package can break DNS answering entirely) -- the same judgment call this row previously recorded. What's real instead, via `apdns-hostagent`: `update.check`/`update.stage`/`update.apply` manage this appliance's own Go binary specifically. A staged candidate's SHA-256 is verified against its claimed checksum *and* the binary is executed with its own `version` subcommand to confirm it self-reports the claimed version before it's ever trusted -- reject otherwise. Apply backs up the current binary first (atomic tmp+rename), swaps in the candidate, restarts, and polls a real health check; a candidate that doesn't come back up healthy is rolled back automatically, restoring and restarting the previous binary. Proven end-to-end against real compiled Go binaries, including a full swap-then-fail-health-check-then-auto-rollback cycle. Not a substitute for Python's real `dpkg` package-level updates, which remain not started | done (for this scope) | done (for this scope) | done for what's built (`hostagent_smoke.mjs`: real current-version renders) |
| Logs (System Logs) | `logs` | `GET /api/logs/units`, `/api/logs/{unit}` | Unit + severity + line-count filter form; **"All" option required** (governing-task requirement -- V2-only enhancement, Python's own log viewer has neither an "All" option nor a severity filter, see `ops_logs.go`'s `AllUnitsSentinel` doc comment) | Log entries table | **done (2026-08-26), including severity filter and the "All units" merge**: `apdns-hostagent` reads the real systemd journal via `journalctl -u <unit> -n <lines> -o json --no-pager [-p <severity>]` (argv-based only) for a fixed compile-time allowlist of units. `unit=all` merges every allowlisted unit's own recent entries (each queried independently, then sorted by time and trimmed to the requested count -- a noisy unit can't starve a quiet one out). Also added: `FileUnits` (tail a plain slog-JSON log file for a process not run as a systemd unit or container -- this preview's own nohup'd `apdns-hostagent`) and `ContainerUnits` (match journald's `CONTAINER_NAME=` field instead of `-u`, for the Go web container). Line count still clamped 1-2000 server-side. Proven against real journal/file/container sources, not synthetic log lines | done (severity dropdown, "All" option showing a Unit column, real 5s auto-tail poll) | not started (no Chromium coverage added for the new controls yet) | not started |
| DNS Runtime | `dnsruntime` | *(new, V2-only -- Python has no equivalent status/apply page; the closest analog is its own systemd `.path`-triggered reload units, which have no UI)* | "Apply Runtime Changes" button | Runtime status (BIND/dnsdist running) | **done, via `internal/dnscompile` + `internal/dnsruntime` + `internal/hostagentd/ops_dnsruntime.go`** -- see the "DNS Runtime Compiler" section above for the full architecture and what it does/doesn't cover yet | **done** -- `DnsRuntimeView.svelte`: real status card, real "Apply Runtime Changes" button showing the actual promoted/rolled-back/error outcome, discloses its own scope in-page | done for what's built (4 Chromium checks in `hostagent_smoke.mjs`: nav/render, real status renders, Apply performs a real compile+promote+report cycle) | not started |

## Cross-cutting / shell-level (Phase 2, not a single page)

| Item | Source | Go API | Svelte | Browser test | Owner accepted |
|---|---|---|---|---|---|
| Nav shell: 4 groups (DNS/Security/Operations/System) + Dashboard, single-open accordion, collapsed flyout, keyboard/pointer/touch | `app.js` GROUP_ORDER + accordion fix history in `v1-workflow-parity-contract.md` | n/a | done -- `Nav.svelte`/`nav.ts`: **single-open accordion** (opening a section always closes whichever was open; the previous "keep multiple open" preference/checkbox was removed entirely, not just defaulted off), a `$effect` on `router.current` opens exactly the current route's own parent section on any navigation (sidebar click, direct deep link, browser back/forward), the collapsed rail's own flyout is inherently single-open, off-canvas drawer <760px shares the identical `openGroupId` state so all three surfaces are always consistent. Collapsed-rail icons are horizontally centered in their button (a real, found-live misalignment: with the label hidden, the icon was left-flush against the button's own padding instead of centered). Arrow-key roving focus not yet added | done -- `nav_accordion_smoke.mjs` (15 checks): open A -> open B closes A, direct-route selection opens only that route's section, collapsed-rail flyout single-open and icon-centering, mobile drawer parity -- plus the full viewport/theme sweep (14 routes x 3 viewports x 2 themes, 84 checks) | not started |
| Frontend deployment integrity (atomic release, no stale-asset breakage across redeploys) | governing task, found live on :10443 | `internal/httpapi/static.go` | done -- real defect found and fixed: `handleStatic` set no `Cache-Control` at all, so a browser's own heuristic caching could hold a stale `index.html` referencing content-hashed JS/CSS chunk filenames a later redeploy (which replaces the static directory wholesale) had genuinely removed, surfacing live as exactly the reported failures (CSS preload failure, dynamic JS import failure) on Query Log/Local DNS/DNS Settings. Fixed: `index.html`/the SPA fallback is always `Cache-Control: no-cache` (always revalidated); everything under `/assets/` is `Cache-Control: public, max-age=31536000, immutable` (safe -- the filename itself changes on any real content change). Deploy procedure itself also made atomic (build to a fresh sibling directory, two `rename(2)`s to swap it in, never `rm -rf` + `cp -r` in place) | done -- 4 Go tests (`static_test.go`, including one reproducing the exact live failure shape: an old `index.html` referencing a since-removed hashed asset) plus `deploy_integrity_smoke.mjs` (62 real-Chromium checks against a disposable instance sharing the exact real deployed release bundle: normal reload, hard reload with cache disabled, all 19 direct deep links, all 19 menu-click routes, zero console/page errors) | not started |
| Shared data grid (sort/resize/persist/overflow) | `data-grid.js` | n/a | done (`DataGrid.svelte`; used by every existing page's tables, replacing ad hoc `<table>` markup) | done -- Chromium proves each mechanic actually works, not just renders: clicking a sortable header changes row order (asc then desc), dragging the resize handle changes the column's real pixel width, and that width survives a full page reload (localStorage per gridId) | not started |
| Theme (light/dark), design tokens, local SVG icons | roadmap "Final Product Experience" | n/a | in progress (light/dark CSS-var tokens carried over from Milestone 1; icon set added this slice; no dedicated design-system pass yet) | done (Chromium: theme toggle flips `data-theme`) | not started |
| Timestamp engine (3 modes, instant reformat) | `app.js` timestamp display | n/a | done (`timestamp.svelte.ts`: reactive singleton, Administration owns the picker; now consumed by every page with a timestamp column -- Backup & Restore, Query Log, System Status's Components/UI Performance, Encryption's TLS validity window) | done (Chromium: mode change updates the on-screen preview instantly) | not started |
| Toasts, dialogs, confirmation flows, action menus (kebab/overflow) | `app.js` shared UI | n/a | not started | not started | not started |
| Route-level code splitting, cancellation, stale-response protection generalized from `staleGuard.ts` | governing task Phase 2 | n/a | done — build-verified (`RouteLoader.svelte` dynamic `import()`; `dist/assets` shows per-page JS/CSS chunks split from the main bundle). `router.svelte.ts`'s per-navigation `AbortSignal` is wired into both existing pages' list fetches (`api.listBlocklists`/`listLocalDNS` now take a signal) -- routing away actually aborts the in-flight request, not just discards its result. Not yet threaded through every mutation call on those pages, and not yet a pattern documented for future pages beyond this example. | not started | not started |

---

## Open questions to resolve against the code (not guessed here)

1. **Logs "All" option**: the inventory found a fixed `LOG_UNITS` select with 8 named units; confirm
   whether an "All" aggregate option exists in Python today or is a new V2 requirement — check
   `app.js` `logs()` and `GET /api/logs/units` response before building the Go equivalent.
2. **Dashboard card customization** (add/remove/reorder/persist): not present in Python V2 per the
   inventory — this is new V2-only scope per the governing task, needs its own small design pass
   before implementation, not an inference from Python behavior.
3. **Query Log → rule creation** and **V1-style custom-rule semantics** (Filters page): locked owner
   decisions in the roadmap, not yet built in Python V2 either. Build against the roadmap's stated
   semantic classes (allow/block/regex/rewrite/precedence/bulk/validate/import), not against a
   Python implementation that doesn't exist yet.

## Sequencing note: the Python compatibility-boundary design gate

Two real technical facts constrain how any Go<->Python compatibility boundary can work safely:

1. **The two backends' sessions are not interchangeable**, despite sharing a cookie name
   (`alderpointdns_v2_session`, deliberately matched for parity) -- each has its own independent
   session table. A naive "forward the incoming cookie to Python" proxy will not authenticate.
2. **"Do not duplicate authoritative state"** (governing task, Phase 4) rules out silently copying
   Python's policy/analytics data into Go's own SQLite -- the boundary has to be a read path
   (direct read-only access to Python's existing stores, or a proxy with its own auth), not a
   second copy.

**Analytics boundary: designed, built, and real-incident-hardened** (`internal/pyanalytics` +
`internal/analyticssnapshot`, 2026-08-26/27). No new auth surface needed at all, because it never
goes through Python's HTTP API or session system in the first place -- fact 1 above is sidestepped
entirely rather than solved.

Current shape (as of commit `9de8fd4`, superseding an earlier direct-read design after a real live
P0 -- see `AGENT_PROGRESS.md`'s incident writeup for the full sequence): the Go web process does
**not** read Python's live `analytics/aggregates.db` directly at all. `apdns-hostagent` (root, real
unrestricted host-path access) periodically produces a real, consistent, standalone copy via
SQLite's own `VACUUM INTO`, publishes it atomically (directory rename + symlink flip,
`internal/analyticssnapshot`), and the web process reads only that published, write-once snapshot
through its own separate, genuinely-read-only mount. The earlier design (a direct `mode=ro`
connection to the live file, mounted read-only) turned out to be structurally incompatible with the
real deployed mount: it is a genuine kernel-enforced read-only bind mount, under which SQLite's own
rollback-journal read path needs a write-class `open()` (checking for a hot journal) that a real
read-only mount refuses outright (`SQLITE_CANTOPEN`) regardless of permission bits or journal mode
-- confirmed live, not assumed, and the reason `immutable=1` existed in an even earlier draft of
this boundary, which then caused the actual corruption incident (a live database is still mutable,
WAL or not -- "it reports wal" was never proof `immutable=1` was safe). The snapshot design resolves
that tension instead of picking a side of it: once published, a generation's files are never written
again, so `immutable=1` is legitimate for the first time, and the read-only mount stays exactly that
-- read-only, at the kernel level, the whole time. Verified against the actual live, actively-written
preview database (not a static copy): a real concurrent regression suite including an in-test real
kernel bind mount (`internal/analyticssnapshot`'s own tests), and a sustained live probe against the
real running :10443 deployment (4850 requests, 0 errors, 0 degraded, exercising the exact two
route/window shapes the incident was filed against). This unblocks Dashboard's DNS-Activity/
Top-Domains data (done, see the Dashboard row) and, going forward, Query Log's underlying data
(still needs its own page built) and any other page that only needs *read-only analytics*.

**Policy domains: native Go, not a boundary at all.** Clients & Access, Filters, and every other
page whose data lives in Python's `control.db` policy tables needs either (a) a native Go schema +
CRUD for that domain (the Blocklists/Local DNS/Upstreams pattern -- Go owns it outright, the actual
end-state per the Definition of Done's "no permanent dependency on the Python web application") or
(b) a *write-capable* boundary, which is a much bigger trust/consistency problem than the read-only
analytics case above (a write needs to go through Python's own validation/staging/promotion, or Go
needs to reimplement that safely itself) and is not something to design in passing. Given the
Definition of Done explicitly rules out a permanent proxy, **native Go schema + CRUD is the intended
target, not a boundary** -- proven now by `internal/upstreams` (real Go tables, real Go service
logic, field-for-field matched against `policy_store.py`, no proxy, no boundary object at all -- see
the DNS Settings/Upstreams row above). Clients & Access is next, same pattern, informed by reading
`policy_model.py`/`policy_store.py`'s client/group/network policy tables for the exact semantics.

## Non-goals carried over from Milestone 1 (still correct, restated here)

Per `docs/v2/v2-roadmap.md`'s Locked Architecture: DNS hot path (dnsdist → BIND → upstream) never
synchronously depends on the control plane, in Go or Python. Runtime workers not yet in Go
(analytics, discovery, replication, schedule, tier-B, protobuf-receiver) are reached through the
existing authoritative SQLite/YAML/compiled-runtime mechanisms per the governing task's Phase 4
compatibility-boundary rule — tracked per-endpoint in this matrix as each page's Go API work lands,
not proxied wholesale through the Python web app.
