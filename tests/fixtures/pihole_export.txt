# adlists.list
https://blocklists.example/adlist.txt
https://blocklists.example/hosts.txt?token=adlisttoken42

# whitelist.txt
allowed-bare.example
whitelist allowed-keyword.example

# blacklist.txt
blocked-bare.example
blacklist blocked-keyword.example
group homework 1
grouped.example,1,2
not a real pihole line %%%

# regex.list
(\.|^)wildcardblock\.example$
regex ^tracker[0-9]+\.example$
regex (\.|^)keywordwild\.example$
regex bad(?=lookahead)\.example

[regex whitelist]
^goodregex\.example$
regex whitelist ^alsogood\.example$

# custom.list
192.168.1.90 printer
fd00::91 nas nas-alias

# cname records
cname=media,printer
cname=slow,printer,600
cname=broken-target,not..valid..

[groups]
adlist 1 https://grouped.example/list.txt
