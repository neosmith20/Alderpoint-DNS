# Alderpoint DNS VM baseline

Captured: 2026-07-28 (America/Denver)

## Platform

- Hostname: `alderpointdns-1`
- OS: Debian GNU/Linux 13.6 (trixie)
- Kernel: `6.12.96+deb13-amd64`
- Architecture: x86_64 KVM virtual machine
- CPU: 4 vCPU (AMD Ryzen Threadripper 2970WX host)
- Memory: 3.8 GiB RAM, 2.6 GiB swap
- Root disk: 47 GiB ext4, 41 GiB available

## Network

- Loopback: `127.0.0.1/8`, `::1/128`
- Interface: `ens18`
- VM address: `172.16.43.101/24`
- IPv6: link-local only (`fe80::9460:9287:904:85fa/64`)
- Default gateway: `172.16.43.1`
- DHCP-managed by `dhcpcd`
- Detected local search domain: `mylan.network`
- Management CIDR: not yet authorized; web service must remain loopback-only until supplied.
- Allowed DNS client networks: not yet authorized; dnsdist must remain loopback-only until supplied.
- DNS hostname: not yet supplied.

## Existing listeners

- TCP 22 on IPv4 and IPv6: OpenSSH
- UDP 68 and UDP 546: DHCP clients
- UDP 44991: unowned/undisclosed kernel socket
- Ports 53, 443, 853, and 3000 were free on both TCP and UDP at capture time.

## Maintenance DNS

`systemd-resolved` is not installed. `/etc/resolv.conf` is generated by `dhcpcd`
and explicitly uses:

- `1.1.1.2`
- `1.0.0.2`

This is the management resolution path and is independent from Alderpoint DNS's BIND
and dnsdist services. `getent ahosts debian.org` succeeded through this path.
Fallback maintenance resolvers will be recorded as `4.2.2.1` and `4.2.2.2`.
Alderpoint DNS download code must use the host resolver and must never rewrite
`/etc/resolv.conf` to point at the client-facing service.

## Security controls

- AppArmor: loaded and active; 105 profiles loaded at capture.
- nftables: no rules were printed (empty ruleset).
- UFW: not installed.
- No Alderpoint DNS ports were exposed.

## Package capabilities at capture

- BIND 9 candidate: `1:9.20.26-1~deb13u1`
- dnsdist candidate: `1.9.15-0+deb13u1`
- Neither package was installed.

Encrypted-DNS protocol capabilities must be tested against the installed dnsdist
binary after installation. In particular, DoQ and DoH3 must not be advertised
unless the Debian build exposes their required functions.

