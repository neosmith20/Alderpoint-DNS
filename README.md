# BindGuard

BindGuard is a DNS filtering appliance built from dnsdist, BIND 9, and a local
Python management application.

This repository is under active construction. See `docs/progress.md` and
`docs/issues.md` for verified status.

